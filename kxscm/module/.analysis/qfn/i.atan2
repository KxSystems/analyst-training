{[y; x]
    : $[x > 0;
            atan y % x;
        x < 0;
            $[  y >= 0;
                atan[y % x] + PI;
                atan[y % x] - PI];
        x = 0;
            $[  y > 0;
                    pi % 2;
                y < 0;
                    neg PI % 2; 0];];
    }