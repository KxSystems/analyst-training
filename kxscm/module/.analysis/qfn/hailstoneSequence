// @fileOverview
// Sequence from the Collatz conjecture
//
// Take any positive integer n. If n is even, divide it by 2 to get 
// n / 2. If n is odd, multiply it by 3 and add 1 to obtain 3n + 1. 
// Repeat the process indefinitely. The conjecture is that no matter 
// what number you start with, you will always eventually reach 1
//
// @param x {long} Any positive integer number
//
// @returns {long[]} A list of all values in the hailstone sequence
{[x]
    // Check for the base case of x = 1
    : $[x = 1;
            1;
        // Otherwise, check if x is odd and repeat.
        // If x is odd then perform 3x + 1, else x / 2
            enlist[x], $[x mod 2; .z.s (3 * x) + 1; .z.s x div 2 ]
        ];
    }
